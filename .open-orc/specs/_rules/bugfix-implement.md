# Rules for workflow: bugfix-implement

Use for bugs, crashes, and regressions.

- Restate the bug in Goal.
- Acceptance criteria must include reproduction + fixed behavior.
- Fill **Repo facts** with the failing path(s), relevant symbols, and verified commands.
- Keep Todo list minimal and grounded in Repo facts.
- Out of scope: new features.
